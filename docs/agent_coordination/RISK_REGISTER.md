# AICCL 风险登记

| Risk ID | 风险 | 证据/触发条件 | 影响 | 严重度 | 可能性 | 缓解与验收 | Owner | 状态 |
|---|---|---|---|---|---|---|---|---|
| R-001 | 当前目录不是有效 Git worktree。 | 根目录 `.git` 为空；`git status --short` 返回 not a git repository；历史报告亦记录相同事实。 | 无法证明干净基线、提交哈希或精确 diff；易把既有修改误认为本轮修改。 | high | certain | 严格文件所有权；记录所有本轮新增文件；不声称工作区干净或存在 commit。 | Main | open |
| R-002 | 默认 Python 无法运行测试。 | Python 3.13.5 执行 `python -m pytest -q` 返回 `No module named pytest`。 | 直接按 README 命令会失败。 | medium | certain | 复用现有 `F:\\AnaConda\\python.exe`，不安装依赖；同时保留原始失败。 | Main/Core | mitigated |
| R-003 | 当前可运行测试环境缺少 Torch。 | Conda 测试结果 `73 passed, 4 skipped`，skip 位于 moment policy shape、optimizer checkpoint、sequence evaluator、partial-demand decoder 等价测试。 | 关键 decoder/模型路径未在本机本轮覆盖；不能宣称 84 项全通过。 | high | certain | Phase 0 如实记录；Phase 1 Gate 需要 Torch 环境独立执行或继续明确为环境缺口。 | Main/Supervisor | open |
| R-004 | partial-demand decoder 的大候选剪枝泄漏真实 demand。 | `SlotDecoder.decode_slot()` 虽构造 `policy_demands`，但候选数超过阈值时 `is_demand_score = demands[...]` 使用真实参数；现有小规模测试没有触发该分支。 | policy 可在 observation 不完整时通过剪枝集合间接使用未揭示目的地。 | critical | high（随规模触发） | Phase 1 不直接复用该路径；新增强制触发 pruning 的反事实泄漏测试，并让所有 policy 路径只读 observation。 | Core/Supervisor | open |
| R-005 | 完整 `ProblemInstance` 本身泄漏 `X_t`。 | 对象公开 `demands`、`traffic_matrix`、`initial_state`、`C`、metadata；现有 runner 在调度前完整构造对象。 | 即便 feature 遮蔽，policy/adapter/cache 仍可读取完整真值、总 chunk 数、source ownership 或 destination。 | critical | certain（若直接传递） | `UncertainProblemInstance` 私有持有 truth，policy-facing `PartialObservationState` 不引用内部 `ProblemInstance`；oracle 单独入口。 | Main/Core/Supervisor | open |
| R-006 | 旧 partial-demand 不是单调 reveal 环境。 | `build_partial_observation()` 生成单个静态 observation；仅 4 modes；ratio 只对两种 mode；无 reveal stage、0/0.75/1 轨迹、最终全揭示、`time_based_arrival`。 | C4 结果不能作为 H1/H2 的有效环境基础。 | critical | certain | Phase 1 新建 `DemandRevealProcess`，测试 mask 单调和 `M_R=all visible`。 | Core | open |
| R-007 | 旧 partial-demand 保留完整 chunk action space并以代理目的地填充。 | 文档/代码明确 chunk identity 与 source ownership 已知；source totals 模式生成 balanced proxy，source+destination totals 通过 max-flow 生成 proxy。 | 隐藏程度被低估；代理需求可能被误当成真实可执行需求。 | high | certain | aggregate 仅作为 observation/scenario 约束；没有精确揭示目的地的 demand 不可执行；假设动作与真实动作类型分离。 | Main/Core/Supervisor | open |
| R-008 | Moment context 的 current z/global 特征可读取当前完整矩阵。 | `SlidingMomentEstimator.get_context(current_matrix, ...)` 用当前 matrix 计算 z；`get_global_moment_features()` 可直接计算当前 sparsity/load/max。 | 若 Phase 1 错传 `X_t`，会绕过 reveal。 | critical | high | Phase 1 不把 Moment action policy 作为方法；任何历史/基线特征只从 `X_{<t}` 或当前 observation 构造；泄漏反事实测试改变隐藏 truth 后 observation 输出必须不变。 | Core/Supervisor | open |
| R-009 | 长期生成器 metadata 含未来 latent/regime/shock 信息。 | `metadata` 存储完整 `latent_regime`、`shock_flags`、`shock_component`、hotspot 轨迹，并标注 audit/evaluation only。 | policy 若接收原 `TrafficSequence.metadata` 可获得未来标签。 | critical | possible | policy API 不暴露 generator metadata；仅 evaluator/oracle audit 可访问；测试对 metadata 能力边界做反射检查。 | Core/Supervisor | open |
| R-010 | evaluator 同时承担真值计分和可能的决策辅助，接口边界模糊。 | `evaluate_schedule()` 正确使用完整 truth 做事后 legality/completion；若调度器直接持有同一 problem，则可偷读 truth。 | “评估可见真值”可能演变为“policy 可见真值”。 | critical | possible | evaluator 只在 schedule 生成后调用；policy 只接收 observation/token；oracle 走隔离函数并清楚标记。 | Main/Core/Supervisor | open |
| R-011 | 现有统计工具不足以满足后续 Gate。 | 旧 V1 某些 CI 按训练 seed 正态近似；Phase 1 尚无 recourse/reveal metrics；行数远大于独立 sequence 数。 | 夸大显著性或选择性报告。 | high | high（若进入 H1/H2） | Phase 1 保留 raw rows 和 sequence ID；H1 以后使用 sequence-cluster bootstrap、paired delta、至少 3 seed、ACF/ESS；本轮不作收益结论。 | Main/Supervisor | open |
| R-012 | legacy generator 精确短周期，外部有效性有限。 | `process_generator.py` 将一个 window period 平铺；历史诊断确认 2/16 周期。 | H1 可能被人工周期夸大。 | high | certain | Phase 1 环境可复用 long-horizon generator；H1 按 family/完整 sequence 切分，并避免 latent metadata。 | Main/Core | open |
| R-013 | `ProblemInstance` 对零 chunk 的形状与 decoder/model兼容性未在 uncertainty reveal 轨迹中验证。 | matrix conversion 允许 `C=0`；ratio 0 或完全等待可能产生空 observation/action set。 | reveal ratio 0 的 baseline 可能崩溃或偷偷用 truth 保持 shape。 | high | high | Phase 1 测试 ratio 0、空 action set、wait 行为和最终 reveal；不得以 truth padding 规避。 | Core | open |
| R-014 | 多 topology 与真实 trace 的结论尚未验证。 | 正式 V1 主要为 Rear4GPU 合成流量。 | Phase 0/1 的合法性可成立，但不能外推性能收益。 | medium | certain | Phase 1 只验证环境语义，不声称算法收益；H1/H2 后续再扩 topology/family。 | Main | accepted limitation |
| R-015 | 现有 deterministic checker 的动作域与 reveal legality 不完整。 | `evaluate_schedule()` 检查 shape、capacity、shared group 和 source possession，但未先强制 `Y_t` 为有限、非负、二值矩阵；也不知道某个 demand 是否已在当前 reveal stage 可执行。 | 非二值/负值可能绕过部分 load 语义；物理可行不等于信息时序合法。 | critical | possible | Phase 1 在旧 checker 外增加 reveal-aware validator，先验证动作域和 revealed token，再调用原 checker；专项测试覆盖负数、非二值、未知 token 和合法已揭示动作。 | Core/Supervisor | open |
| R-016 | Phase 1 初版 baseline/metrics 可能“接口完整但语义为空”。 | 初版绿色代码中 Long-term mean/Previous-value 直接丢弃 history；direct proposal 未按 edge/shared capacity 筛选；`completion` 写入完成 token 数且 regret/recourse/timeout 为常量。 | Gate 可被表面字段和宽松 fixture 误导；后续 H1/H2 对照与 paired regret 无效，低容量 topology 可能只得到 validator 异常而非合法 schedule。 | critical | certain（初版） | P1-BASE-HARDEN-001 已闭环；Main/Supervisor 独立测试与代码审查通过。 | Main/Core/Supervisor | mitigated |
| R-017 | 固定顺序 greedy full-information comparator 不保证支配 partial 方法。 | Supervisor chain6/seed2 反例：oracle completion 11，partial/mean/previous 10，regret -1。 | `oracle_regret` 失去非负上界含义，可能把 action ordering 效应误报为 uncertainty cost。 | critical | proven | 已改为 §11.8 有证明的 non-executable completion lower bound；chain oracle7/ordinary10、regret非负，未 clamp；Supervisor 证明/反例/600 paired 复审通过。 | Main/Core/Supervisor | mitigated |
| R-018 | topology/config digest 可与实际 paired 输入不一致。 | runner 只校验 truth digest；bogus topology/config 字符串被接受并原样进入 raw row。 | raw provenance 不能证明方法使用相同真实 topology/config。 | high | proven | canonical factory 与 runner 三摘要复核已实现；Supervisor 对 bogus topology/config 与 mutated config 三类 probe 均确认拒绝。 | Main/Core/Supervisor | mitigated |
| R-019 | 旧 predictability 证据不能直接通过 H1。 | 旧方法缺 EWMA/MLP/GRU-or-TCN/quantile-scenario/LOFO；bootstrap 比较 constant 而非 previous-value。 | 可能把 1.7% 的表面 RMSE 改善或大量相关 step 误报为稳定增量信息。 | critical | certain | 新建独立H1协议/实现/formal raw；正式结果否定旧表面改善。 | Main/Core/Supervisor | mitigated |
| R-020 | 当前环境无 Torch，必需序列神经模型可能被静默跳过或虚假命名。 | import probe：Torch/TensorFlow/JAX 均不可用。 | 模型对照不完整，H1 结论失效。 | high | certain | 已实现并测试真实可学习NumPy causal TCN：gradient/kernel/loss/causality/round-trip均过。 | Core/Supervisor | mitigated |
| R-021 | generator metadata 泄漏未来 regime/shock/hotspot。 | `LongHorizonTrafficSequence.metadata` 保存完整 latent 轨迹。 | predictor 可获得无法在 `t` 时刻观测的未来状态，夸大 H1。 | critical | possible | builder不读取metadata；current/future/metadata反事实测试通过；runner只传history arrays。 | Core/Supervisor | mitigated |
| R-022 | test-set 模型选择或校准造成乐观偏差。 | H1 同时比较多模型和概率覆盖，若只做 train/test 容易在 test 上选 backbone/分位数。 | CI 和 coverage 不再是 held-out 证据。 | critical | high | 四完整sequence split；validation选MLP、calibration拟合residual、test一次评估；raw可重算。 | Main/Core/Supervisor | mitigated |
| R-023 | 相邻时间步伪独立与 family/seed 聚集夸大显著性。 | 正式 mean lag-1 ACF 0.8714。 | step-level CI 过窄。 | critical | certain | 每sequence聚合、family-stratified sequence bootstrap10k；报告15独立sequence/ESS。 | Main/Supervisor | mitigated |
| R-024 | pooled in-family 提升不能证明未见动态可泛化。 | pooled只有1/5 family正；LOFO 0/5正且3 family恶化>10%；Supervisor最终复算确认。 | Phase 3A 场景路线在分布转移下系统失败。 | high | proven | H1最终判FAIL并建议Phase3B；不得进入predictive scenario路线，任何Phase3实施等待用户授权。 | Main/Core/Supervisor | open route blocker |
| R-025 | 绿色模型原语测试可能被误当成完整 H1 实验证据。 | 初版H1 package无formal runner/完整raw。 | 无法审计Gate。 | critical | proven | pipeline red/green、schema2 manifest、两raw表、summary重算与PENDING_SUPERVISOR均完成。 | Main/Core/Supervisor | mitigated |
| R-027 | 正式 pooled/LOFO MLP 的部分拟合达到预注册80 iteration上限而未满足sklearn收敛阈值。 | 正式命令产生3个ConvergenceWarning；toy测试另有固定告警。 | MLP是固定预算下结果，不能声称优化收敛；事后增加epochs会破坏预注册。 | medium | proven | 不调参、不重跑择优；保留warning并把结论限定为冻结的80-iteration候选；H1已FAIL。 | Main/Supervisor | accepted limitation |
| R-026 | 内存 sequence 与 manifest digest、非关键 generator config 字段未在通用 `_execute_experiment` 入口逐值二次复核。 | Main/Supervisor green源码审查；formal public path 同步生成→digest→manifest→执行且CLI不可覆盖，但离线调用可替换对象或同时篡改完整config。 | 不阻塞本次canonical formal run；降低通用离线抗篡改强度。 | medium | possible | 本次只使用public frozen runner；保留source/protocol/topology/group/75 sequence digests；实验后列入非阻塞hardening，不据此扩大结论。 | Main/Supervisor | accepted limitation |
| R-028 | Phase3B ordinary ambiguity constructor 可能通过observation provenance、family/sequence ID或evaluator引用读取不可观测信息。 | 当前`PartialObservationState`公开payload含family/sequence_id，ScenarioSet本身没有能力边界。 | family-specific集合或truth泄漏会伪造coverage/support质量。 | critical | possible | 新建剥离标识的窄只读ConstructionView；family/ID/latent/future counterfactual与forbidden API/import tests；oracle独立调用栈。 | Main/Core/Supervisor | implementation mitigated; formal/final audit pending |
| R-029 | 历史candidate与当前exact entries/totals/partial-shard lower bounds矛盾。 | 直接把recent matrices放入ScenarioSet不会校验当前observation。 | ambiguity support不可作为合法当前不确定变量，ratio1也可能不退化。 | critical | certain | 协议与实现已覆盖entry≤8、source capped largest-remainder、source+destination candidate-sensitive integer min-cost flow、统一validator与ratio1 singleton；Core/Main green，formal raw仍待最终审计。 | Core/Supervisor | implementation mitigated; formal audit pending |
| R-030 | 复用H1 test或在test上选择selector/K/radius导致二次使用和乐观偏差。 | H1 corpus已用于选择3B路线，五法和K又形成多重候选。 | Phase3B确认性结论无效。 | critical | certain without split | 新342/442/542 fresh corpus；whole-sequence fit/validation/calibration/test；validation只在K8选方法，calibration只定radius，test一次。 | Main/Core/Supervisor | mitigated by protocol |
| R-031 | oracle support直接包含X_t会成为泄漏基线或影响普通方法。 | oracle插入truth可得到零nearest distance，是tautological ceiling。 | 被误当可部署方法或用于调参会污染Gate/Phase4。 | critical | certain | 强制uses_oracle/upper_bound_only；不参与selection/条件/Phase4输入；单独evaluator与counterfactual；ratio1仍为actual1。 | Core/Supervisor | implementation mitigated; formal/final audit pending |
| R-032 | Phase3B滑入prefix/recourse或以traffic-space指标宣称AICCL调度收益。 | 总路线Phase3B与Phase4相邻，现有execution/evaluation接口可被误用。 | 研究Gate混淆、绕过H2。 | critical | possible | 模块禁止import execution/Proposal/commit；协议禁止completion/regret/recourse结论；静态/fresh-process测试通过，最终仍必须停止在Phase4前。 | Main/Core/Supervisor | implementation mitigated; final audit pending |
| R-033 | 只有mean/variance区间文字但没有概率权重集合，可能把无限pure-moments region误称有限经验ambiguity。 | Supervisor首审确认初版缺full-support distribution语义与非空witness。 | 集合可能为空/不可计算，或K子集与distribution约束混为一谈。 | critical | proven | 已实现32-index `P(A)`、componentwise mean/variance约束、1e-10容差和uniform非空witness，并分离point envelope/full-32 weights/K-support；Core/Main专项green。 | Main/Core/Supervisor | implementation mitigated; formal audit pending |
| R-034 | tail event不足可能遮蔽其他已明确失败。 | H1曾需要专门修复tail masking；Phase3B初版只写不足10为HOLD。 | 明确FAIL被错误改写为HOLD。 | high | possible | 冻结并测试优先级：其他条件任一FAIL则总FAIL；只有其余全过且tail不足才HOLD；fractional random hit亦已验证。 | Main/Core/Supervisor | mitigated |
| R-035 | Phase3B artifact看似可重算但只校验nearest/count，manifest派生字段与digest可联动伪造。 | Supervisor完整120000/300 probe：改joint coverage仍ACCEPT；tail/width/sequence recall及manifest+summary联动篡改通过；所有row digest同为64字符亦通过。 | Gate条件1--6、selector/LOFO/dependence和provenance不能由formal artifact独立审计，可能把损坏/伪造证据误判PASS。 | critical | proven | schema v2新增calibration/validation/LOFO/dependence raw、十artifact精确宇宙、全字段domain/hex/provenance绑定、raw→sequence→Gate→summary唯一重算、logical digests、atomic staging及逐字段corruption red tests；Core/Main双重165-case与全仓green、toy十文件一致、fold/seed负向探针均已通过。 | Main/Core/Supervisor | mitigated in production; second formal admission pending; formal blocked |
| R-036 | Phase3B冻结formal路径运行时间超过当前单命令30分钟工具上限。 | 首次获准`--formal`在`1800.5s`被外部工具终止；后续完整成功运行耗时`3474.8s`，验证Core保守55--80分钟估计。 | 短运行窗口会终止合法内存构建并无法产生正式evidence。 | high | proven | 经事故审查使用120分钟窗口；最终新冻结formal在约57.9分钟exit0、十文件final read-back通过。保留历史用于未来运行预算，不自动重跑。 | Main/Supervisor | mitigated; formal complete |
| R-037 | Phase3B schema-v2 formal converter未保留legacy test row的`requested_k`，而沿用toy helper默认8。 | 实际formal provisional raw中K1/4/16均被写成requested_k=8、actual_k分别1/4/16，合计72,000 unknown行；ratio1另有18,000非K8行被隐性误标；首次read-back以`unknown-ratio actual K must equal requested K`拒绝。源码 `_v2_convert_test_rows` 覆盖actual_k但未覆盖requested_k。 | 正式证据无法通过schema验证；case与正确保留四K的300行sequence aggregate错位；若无validator则会污染全部多K比较及Gate选择。 | critical | proven | 40-row真实converter multi-K test先由Core/Main双重TRUE RED；Supervisor授权严格单行复制修复。三方green与新准入后正式运行exit0，final read-back精确接受120,000/300多K universe。 | Main/Core/Supervisor | mitigated; formal complete |
| R-038 | Phase4可能把scenario/未揭示token绑定成可执行truth action。 | Phase3B support只有aggregate matrix与scenario token namespace，无未知truth身份；提前绑定会读取private map。 | 未来信息泄漏、非法发送，H2结论无效。 | critical | certain without guard | ordinary candidate只来自当前revealed TruthToken；scenario只评分；ratio0 executed=0；所有commit走原checker；专项反事实/namespace RED。 | Main/Core/Supervisor | protocol mitigated; implementation pending |
| R-039 | 多slot prefix/repair可能撤销已执行动作、重发token或从initial state重启。 | 现有Proposal/runner只有单slot，没有suffix lifecycle。 | completion/recourse失真，甚至违反possession/state_version。 | critical | high | immutable executed ledger；每slot只commit第一batch；reveal只替换unexecuted suffix；fresh observation/current holders/state_version；stale plan RED。 | Core/Supervisor | open |
| R-040 | 现有Phase1 `recourse_count/replanned_actions/wasted_plan`语义被误用为H2指标。 | 现有waste是scenario L1，recourse是proposal signature变化，并非prefix lifecycle。 | 不能回答executed/replaced/repair/recovery，Gate可被表面字段误导。 | high | certain | Phase4专用raw schema与手算counterexample；旧字段不静默复用。 | Main/Core/Supervisor | protocol mitigated; implementation pending |
| R-041 | reveal每slot前进使P>1在收到新信息前无法执行。 | Phase1 `stage=min(slot,4)`，full reveal slot4。 | prefix horizon比较退化，Wait差距被人为缩短。 | high | certain | Phase4 paired cadence固定4 slots/stage且所有方法一致；每slotfresh state，stage按floor(slot/4)；Phase5前不事后扫描。 | Main/Core/Supervisor | protocol mitigated; implementation pending |
| R-042 | algorithm overhead与completion slots单位不一致或漏计ambiguity/replan。 | Python robust construction/planning可能远慢于baseline；现有timeout不是wall enforcement。 | scheduling-only改善被误报为端到端收益。 | critical | high | 1ms/slot主模型；全链wall timing+真实deadline；报告break-even与timing tails；H2 PASS要求end-to-end CI正且timeout不增。 | Main/Supervisor | protocol mitigated; measurement pending |
| R-043 | 复用Phase3B test或在H/P/lambda/test comparator上选择造成二次使用。 | P3B test已用于Phase4路线判定；10个合法H/P×3lambda=30个robust配置形成多重候选。 | H2 confirmatory效果乐观。 | critical | certain without split | 新642/742/842、fit/validation/test共45 whole sequences且digest排除H1/P3B；validation冻结配置与primary comparator；test一次。 | Main/Core/Supervisor | protocol mitigated; implementation pending |
| R-044 | 本阶段仍使用NumPy reveal-aware token scheduler proxy，而非legacy Torch AICCL solver。 | legacy decoder含完整chunk模型、truth-dependent>512 pruning与静态partial语义，不能安全直接接入；现环境Torch tests仍skip。 | 即使H2 PASS也可能被过度表述为完整生产AICCL收益。 | high | certain | 第一版保持安全token execution内核；结果严格称AICCL uncertainty scheduling proxy/Gate2证据；不得外推legacy model或真实部署。 | Main/Supervisor | accepted limitation |
| R-045 | Phase4协议初稿仍有机械实现分叉，RED可能锁错合同。 | Supervisor初审14项HOLD；Core确认H/P合法pair仅10组、cadence API/seed/H1 mapping/deadline/schema等未封闭。 | 测试替Main作研究选择，后续green无科学意义。 | critical | proven | Main两轮protocol return冻结全部机械语义；Supervisor test-only ALLOW；Core/Main独立验证最终97项RED、两份SHA及formal空目录，失败只因计划内package缺失。生产实现仍须独立准入与green审计。 | Main/Supervisor | RED mitigated; production admission pending |
| R-046 | Phase4测试可通过production helper自报、自改、自验形成self-attestation，或用未命中约束的弱反例假装覆盖。 | Supervisor对97项RED静态审计发现反事实、oracle binding、group/multi-holder、residual Q、repair、event、artifact、Gate和provenance共9类可被硬编码/协同作弊绕过。 | clean RED仍可能导向伪green，无法证明信息隔离、物理可行性、不可逆执行和artifact证据链。 | critical | proven | 两份test已强化为真实paired inputs/commit轨迹、手工物理反例、直接linked tamper、独立hash图、I/O spy及逐Gate plain evidence；Main通读并独立验证112项clean RED，另修正wall双timeout与exact radius。仍待Supervisor准入复审。 | Main/Core/Supervisor | test mitigation complete; production review pending |
| R-047 | 五stage episode用单一`actual_k`会掩盖robust support从unknown K8到full K1的动态变化。 | Supervisor复审发现episode跨stage但event无K，既可填1也可填8；曾发生Phase3B requested/actual K正式证据事故。 | formal artifact可能无法证明ratio1 singleton或逐stage使用正确support，Gate8 provenance失效。 | critical | proven | exact schema改为episode `actual_k_min/max`，每个`plan_built`落requested/actual K并进入payload digest；124项RED已覆盖method/stage域、event重算、support link和直接tamper。 | Main/Core/Supervisor | protocol/RED mitigated; production pending |
| R-048 | method-order函数确定性与fresh object identity不足以证明paired运行不受oracle/cache/RNG顺序污染。 | reverse-order production self-probe删除后，测试未用真实runner按正反顺序执行同一coordinate。 | 普通方法结果可能因oracle first/last或共享mutable状态改变，paired比较失效。 | critical | proven | test侧同一manifest以18个fresh world/cache实际执行forward oracle-last与reverse oracle-first，逐method比较去timing/order/identity后的scientific rows、final world、RNG与cache key；Main静态验收。 | Core/Supervisor | RED mitigated; green pending |
| R-049 | 冻结RED本身在production green阶段可暴露非法fixture或无变异负例。 | 首次实现后robust唯一失败在旧ScenarioToken构造；experiment counter负例把2-action正确值2仍替换为2。 | 合法实现无法全green，若为迎合测试越界修改旧类型/validator会破坏已冻结语义。 | high | proven | Core立即暂停；Main复现；Supervisor只授权两字面量精确修正并预计算SHA；Main实际SHA完全匹配、collect124与targeted2 pass后才恢复同一生产范围。 | Main/Core/Supervisor | mitigated |
| R-050 | Phase4实现可通过满足公开test API的stub/dummy/constant获得全green，却不执行冻结研究协议。 | 首轮124/full green后Main发现SimpleNamespace假episode、恒81 runner、3/45 seeds、event digest/reason错误、重复prefix batch、physical-hash-only readback等。 | 产生无法运行formal或不可审计的伪生产实现，若直接准入会使H2证据无效。 | critical | proven | Main把测试绿降为必要条件并做源码/反例审计；退回同一六文件实现真实world、H-slot状态模拟、原checker、exact schema/event/stat/H1/artifact recomputation；Supervisor formal准入前必须复核Main probes。 | Main/Core/Supervisor | mitigation in progress |
| R-051 | Phase4真实episode执行与artifact validator可能是两条不相交路径，toy合成表可绿但formal无法生成或无法自验。 | RETURN-002中真实runner存在，但无正式实验入口；真实proposal/action/batch事件缺冻结transaction字段，而toy writer以固定synthetic rows满足公开测试。 | formal准入后才暴露无可执行命令或ledger readback失败；更糟时合成证据替代真实执行结果。 | critical | proven | Main在formal准入前源码审计拒绝；RETURN-003要求单一canonical event builder、真实45/9600/2700/135/21600宇宙与八artifact原子链、九方法真实ledger自验。 | Main/Core/Supervisor | mitigation in progress |
| R-052 | 仅用toy九方法与公开测试无法命中formal专用H1、provisional发布和异常终止cursor。 | RETURN-003全绿但formal H1访问不存在字段；正式publisher只有final readback；illegal/wall统一end slot80；reason/sentinel域漂移。 | 长达小时级正式运行可能晚期崩溃，或生成不符合冻结证据状态机的产物。 | critical | proven | formal运行前强制formal-style H1真实分支、两阶段publisher spy、checker/wall/lower-bound cursor/reason/sentinel独立探针；Main复核后才可请求Supervisor。 | Main/Core/Supervisor | mitigation in progress |
| R-053 | Phase4正式运行时长远高于测试，且cooperative 10s不是硬上界。 | 12,300 episodes、最多984,000 slots；toy估算约1.5--3小时，名义deadline包络34.17小时；单个NumPy/sklearn调用不可抢占。 | 过短outer window会终止唯一合法运行并留下forensic staging；自动重跑会破坏预注册唯一性。 | high | proven | Supervisor准入必须冻结单次命令、目标、staging、process和足够outer window；异常只HOLD并停止，不自动重跑。 | Main/Supervisor | pending formal admission |
| R-054 | Windows PowerShell把多行Python文本作为native `-c`参数时可能剥离代码内双引号。 | 首次冻结调用在0.8s Python解析期把`Path(r"...")`变成`Path(rF:\...)`，exit1；无runner副作用。 | 未做argv层probe会在正式入口前失败；若自动修正重跑会违反单次准入。 | high | proven | 已立即HOLD并核对destination/staging/process全空；readmission须冻结环境变量承载源码、短固定`-c` loader或其他经argv探针验证的包装；不得改源码/科学合同。 | Main/Supervisor | pending readmission |
| R-055 | 正式test coordinate中oracle stage0--3的raw recent-history scenario可能小于当前已揭示token计数。 | 唯一readmitted formal在`project_residual_scenario` fail closed；真实4节点partial-shards RED复现stage1 undercoverage。最小修复后Main独立确认该用例、focused125和full494均通过，测试spy覆盖stages1--4、每个member coverage、ordinary隔离、stage4 singleton与strict ledger。 | 若回退为raw history、忽略或catch/drop scenario，仍会破坏residual load、oracle定义与信息隔离。 | critical | proven | `phase4_experiment.py`已改为oracle stages0--3复用Phase3B observation reconciliation及既有确定性K≤8 selector；`robust_prefix.py` fail-closed未动。正式证据仍须经新Supervisor准入和完整formal验证。 | Main/Core/Supervisor | production mitigated; formal confirmation pending |
| R-056 | 小时级formal若绑定Codex exec/SSH前台，会因客户端交互或会话生命周期终止。 | 第三次formal运行中`functions.wait=aborted`，随后Python0、destination false、staging0；无checkpoint，计算全部丢失。 | 反复中断会消耗单次授权且无法形成科学证据。 | critical | proven | 远端采用project外control、`nohup+setsid+flock+timeout`、无auto-restart；20秒单Python演练断开/重连同PID并自然exit0；正式只读监控，连接丢失先查PID绝不重启。 | Main/Supervisor | remote mechanism prepared; admission pending |
| R-057 | Windows与Linux的Python patch、BLAS和路径差异可能改变正式结果或破坏授权hash。 | 远端Python3.12.3/Linux/OpenBLAS与本地Python3.12.7/Windows不同；原D841 Windows wrapper路径不可复用。 | 若不冻结环境/新wrapper，可能把平台漂移当科学效应或在preflight前失败。 | high | proven | 固定NumPy1.26.4/SciPy1.13.1/sklearn1.5.1/joblib1.4.2/threadpoolctl3.5.0与单线程环境；远端full结果计数完全一致；新remote code/loader/launcher分别冻结SHA，旧D841明确失效。 | Main/Supervisor | preparation mitigated; formal evidence pending |
